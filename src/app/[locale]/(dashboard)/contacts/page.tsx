import { getLocale,getTranslations } from "next-intl/server";
import { Search,UsersRound,Mail,Phone,ExternalLink } from "lucide-react";
import { getContactDisplayName,getInitials,sanitizeSearchTerm } from "@/lib/crm";
import { createClient } from "@/lib/supabase/server";
import { ContactCreateDialog } from "@/components/forms/ContactCreateDialog";
import { ContactEditDialog } from "@/components/forms/ContactEditDialog";
import { DeleteEntityButton } from "@/components/actions/DeleteEntityButton";
import { Card,CardContent } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Button,buttonVariants } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Link } from "@/i18n/navigation";
import { cn } from "@/lib/utils";
export default async function ContactsPage({searchParams}:{searchParams?:Promise<{action?:string;q?:string}>}){const params=(await searchParams)??{};const locale=(await getLocale()) as "en"|"ar";const t=await getTranslations("Contacts"),tc=await getTranslations("Common"),tn=await getTranslations("Navigation");const supabase=await createClient();let query=supabase.from("contacts").select("id,type,status,first_name,last_name,company_name,email,phone,website,location,timezone,notes").order("created_at",{ascending:false});const q=params.q?sanitizeSearchTerm(params.q):"";if(q)query=query.or(`first_name.ilike.%${q}%,last_name.ilike.%${q}%,company_name.ilike.%${q}%,email.ilike.%${q}%`);const{data:contacts,error}=await query;return <div className="space-y-6"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary-light">Nexus CRM</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{tn("contacts")}</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">{t("subtitle")}</p></div><ContactCreateDialog autoOpen={params.action==="new"}/></div><div className="flex flex-col gap-3 sm:flex-row"><form className="relative flex-1 sm:max-w-xl"><Search className="absolute start-4 top-1/2 size-4.5 -translate-y-1/2 text-muted-foreground"/><Input name="q" type="search" aria-label={t("searchPlaceholder")} defaultValue={params.q??""} placeholder={t("searchPlaceholder")} className="h-12 rounded-2xl ps-11"/><Button type="submit" className="sr-only">{tc("search")}</Button></form><div className="hidden items-center rounded-2xl border border-border bg-surface/60 px-4 text-xs text-muted-foreground sm:flex"><UsersRound className="me-2 size-4 text-primary-light"/>{contacts?.length??0} {t("title")}</div></div>{error?<Card><CardContent className="p-8 text-sm text-danger">{tc("retry")}</CardContent></Card>:(contacts??[]).length===0?<Card className="border-dashed"><CardContent className="flex min-h-64 flex-col items-center justify-center p-8 text-center"><div className="flex size-14 items-center justify-center rounded-2xl bg-primary/10 text-primary"><UsersRound className="size-6"/></div><h2 className="mt-4 text-base font-semibold">{t("noContacts")}</h2><p className="mt-1 text-sm text-muted-foreground">{t("createDescription")}</p></CardContent></Card>:<div className="grid gap-4 xl:grid-cols-3 2xl:grid-cols-4">{contacts!.map(contact=>{const name=getContactDisplayName(contact)||tc("unknown");return <Card key={contact.id} className="group overflow-hidden rounded-2xl border-border/80 bg-card/95 transition-all hover:-translate-y-0.5 hover:border-primary/25 hover:shadow-[0_18px_50px_rgb(0_0_0_/_0.18)]"><CardContent className="p-5"><div className="flex items-start justify-between gap-3"><div className="flex min-w-0 items-center gap-3"><span className="flex size-12 shrink-0 items-center justify-center rounded-2xl bg-gradient-to-br from-primary/25 to-accent/10 text-sm font-semibold text-primary-light">{getInitials(name)}</span><div className="min-w-0"><Link href={`/contacts/${contact.id}`} className="block truncate font-semibold hover:text-primary-light">{name}</Link><p className="truncate text-xs text-muted-foreground">{contact.type==="company"?t("types.company"):t("types.person")}{contact.location?` · ${contact.location}`:""}</p></div></div><div className="flex items-center gap-1 opacity-100 transition-opacity md:opacity-0 md:group-hover:opacity-100"><ContactEditDialog contact={contact} compact/><DeleteEntityButton entity="contact" id={contact.id}/><Link href={`/contacts/${contact.id}`} className={buttonVariants({variant:"ghost",size:"icon-sm"})} aria-label={tc("viewDetails")}><ExternalLink/></Link></div></div><div className="mt-5 flex flex-wrap gap-2"><Badge variant="outline" className={cn(contact.status==="active"&&"border-success/30 bg-success/5 text-success")}>{t(`statuses.${contact.status}`)}</Badge></div><div className="mt-5 space-y-2.5 text-sm text-muted-foreground">{contact.email&&<div className="flex items-center gap-2.5"><Mail className="size-4 text-text-muted"/><span className="truncate">{contact.email}</span></div>}{contact.phone&&<div className="flex items-center gap-2.5"><Phone className="size-4 text-text-muted"/><span className="truncate">{contact.phone}</span></div>}{!contact.email&&!contact.phone&&<div className="rounded-xl bg-muted/30 px-3 py-2 text-xs">{t("noDirectInfo")}</div>}</div></CardContent></Card>})}</div>}</div>}
