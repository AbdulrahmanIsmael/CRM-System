import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
import { CalendarDays,Clock3 } from "lucide-react";
import { useLocale,useTranslations } from "next-intl";
import { formatDateTime } from "@/lib/crm";
import { Link } from "@/i18n/navigation";
export interface UpcomingEventData{id:string;title:string;startTime:Date;endTime:Date;context:string|null}
export function UpcomingEvents({events}:{events:UpcomingEventData[]}){const t=useTranslations("Dashboard"),tc=useTranslations("Common");const locale=useLocale() as "en"|"ar";return <Card className="h-full min-h-[250px] rounded-2xl"><CardHeader className="flex flex-row items-center justify-between pb-2"><CardTitle className="text-sm">{t("upcomingEvents")}</CardTitle><Link href="/calendar" className="text-xs font-medium text-primary-light hover:text-primary">{tc("viewAll")}</Link></CardHeader><CardContent>{events.length===0?<div className="flex min-h-[180px] items-center justify-center text-sm text-muted-foreground">{t("noEvents")}</div>:<div className="space-y-2">{events.map(e=><div key={e.id} className="rounded-xl border border-border bg-surface/40 p-3"><div className="flex items-start gap-3"><span className="rounded-lg bg-accent/10 p-2 text-accent"><CalendarDays className="size-4"/></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{e.title}</p><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><Clock3 className="size-3"/>{formatDateTime(e.startTime,locale)}</p>{e.context&&<p className="mt-1 truncate text-xs text-muted-foreground">{e.context}</p>}</div></div></div>)}</div>}</CardContent></Card>}
