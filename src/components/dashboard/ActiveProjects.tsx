import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { FolderKanban } from "lucide-react";
import { Link } from "@/i18n/navigation";
import { useTranslations } from "next-intl";
export interface ActiveProjectData{id:string;name:string;clientName:string;status:string;progress:number|null}
export function ActiveProjects({projects}:{projects:ActiveProjectData[]}){const t=useTranslations("Dashboard"),tp=useTranslations("Projects"),tc=useTranslations("Common");return <Card className="h-full rounded-2xl"><CardHeader className="flex flex-row items-center justify-between pb-2"><CardTitle className="text-sm">{t("activeProjectsWidget")}</CardTitle><Link href="/projects" className="text-xs font-medium text-primary-light">{tc("viewAll")}</Link></CardHeader><CardContent>{projects.length===0?<div className="py-12 text-center text-sm text-muted-foreground">{t("noProjects")}</div>:<div className="space-y-3">{projects.map(p=><Link key={p.id} href={`/projects/${p.id}`} className="flex items-center gap-3 rounded-xl p-2 hover:bg-muted/40"><span className="rounded-xl bg-primary/10 p-2.5 text-primary-light"><FolderKanban className="size-4.5"/></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{p.name}</p><p className="truncate text-xs text-muted-foreground">{p.clientName}</p>{p.progress!==null?<div className="mt-2 flex items-center gap-2"><div className="h-1.5 flex-1 rounded-full bg-surface-light"><div className="h-full rounded-full bg-primary" style={{width:`${p.progress}%`}}/></div><span className="text-[10px] text-muted-foreground">{Math.round(p.progress)}%</span></div>:<p className="mt-2 text-[11px] text-muted-foreground">{tp("noTasks")}</p>}</div><Badge variant="outline" className="shrink-0 text-[10px]">{tp(`statuses.${p.status}`)}</Badge></Link>)}</div>}</CardContent></Card>}
