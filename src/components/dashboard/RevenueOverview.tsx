"use client";
import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
import { useLocale,useTranslations } from "next-intl";
import { ResponsiveContainer,ComposedChart,CartesianGrid,XAxis,YAxis,Tooltip,Bar,Line } from "recharts";
import { formatCurrency } from "@/lib/crm";
export interface RevenueDataPoint{name:string;revenue:number;invoices:number}
export function RevenueOverview({data,currency}:{data:RevenueDataPoint[];currency:string}){const t=useTranslations("Dashboard");const locale=useLocale() as "en"|"ar";const hasData=data.some((point)=>point.revenue>0||point.invoices>0);return <Card className="h-full rounded-2xl"><CardHeader className="pb-2"><div className="flex items-center justify-between"><div><CardTitle className="text-sm">{t("revenueOverview")}</CardTitle><p className="mt-1 text-xs text-muted-foreground">{t("lastSixMonths")}</p></div><div className="flex gap-4 text-xs text-muted-foreground"><span className="flex items-center gap-1.5"><span className="size-2 rounded-full bg-primary"/>{t("revenue")}</span><span className="flex items-center gap-1.5"><span className="size-2 rounded-full bg-accent"/>{t("paidInvoices")}</span></div></div></CardHeader><CardContent><div className="h-[280px] w-full">{(!data.length||!hasData)?<div className="flex h-full items-center justify-center text-sm text-muted-foreground">{t("noRevenueData")}</div>:<ResponsiveContainer width="100%" height="100%"><ComposedChart data={data} margin={{top:10,right:8,left:-12,bottom:0}}><CartesianGrid stroke="var(--color-border)" strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name" axisLine={false} tickLine={false} tick={{fill:"var(--color-text-muted)",fontSize:11}}/><YAxis axisLine={false} tickLine={false} tick={{fill:"var(--color-text-muted)",fontSize:11}} tickFormatter={(v)=>new Intl.NumberFormat(locale==="ar"?"ar-EG":"en-US",{notation:"compact",maximumFractionDigits:1}).format(v)}/><Tooltip contentStyle={{background:"var(--color-surface)",border:"1px solid var(--color-border)",borderRadius:12,color:"var(--color-text-primary)"}} labelStyle={{color:"var(--color-text-secondary)"}} formatter={(value, name) => [name === "revenue" ? formatCurrency(Number(value), currency, locale) : String(value), name === "revenue" ? t("revenue") : t("paidInvoices")]}/><Bar dataKey="revenue" fill="var(--color-primary)" radius={[6,6,0,0]} barSize={26}/><Line type="monotone" dataKey="invoices" stroke="var(--color-accent)" strokeWidth={2.5} dot={{r:3,fill:"var(--color-accent)"}} activeDot={{r:5}}/></ComposedChart></ResponsiveContainer>}</div></CardContent></Card>}
