import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
import { Link } from "@/i18n/navigation";
import { CircleCheck, FileText, FolderKanban, ListChecks, UserRound } from "lucide-react";
import { useLocale,useTranslations } from "next-intl";
import { formatRelative } from "@/lib/crm";
export interface ActivityData{id:string;description:string|null;entityName:string;time:Date;type:string;action:string}
export function RecentActivity({activities}:{activities:ActivityData[]}){const t=useTranslations("Dashboard"),tc=useTranslations("Common");const locale=useLocale() as "en"|"ar";const iconFor=(type:string)=>type==="contact"?UserRound:type==="deal"?CircleCheck:type==="project"?FolderKanban:type==="invoice"?FileText:ListChecks;const entityKey=(type:string)=>`entity${type.charAt(0).toUpperCase()+type.slice(1)}` as "entityContact"|"entityDeal"|"entityProject"|"entityInvoice"|"entityTask";return <Card className="h-full rounded-2xl"><CardHeader className="flex flex-row items-center justify-between pb-2"><CardTitle className="text-sm">{t("recentActivity")}</CardTitle><span className="text-xs text-muted-foreground">{activities.length}</span></CardHeader><CardContent>{activities.length===0?<div className="py-12 text-center text-sm text-muted-foreground">{t("noActivity")}</div>:<div className="space-y-1">{activities.map(a=>{const Icon=iconFor(a.type);const desc=a.description||(a.action==="created"?t("activityCreated",{entity:t(entityKey(a.type))}):a.action==="updated"?t("activityUpdated",{entity:t(entityKey(a.type))}):a.action==="deleted"?t("activityDeleted",{entity:t(entityKey(a.type))}):a.action==="status_changed"?t("activityStatusChanged",{entity:t(entityKey(a.type))}):t("activityStageChanged",{entity:t(entityKey(a.type))}));return <div key={a.id} className="flex items-start gap-3 rounded-xl px-2 py-2"><span className="mt-0.5 rounded-lg bg-muted p-2 text-primary-light"><Icon className="size-3.5"/></span><div className="min-w-0 flex-1"><p className="text-sm text-foreground">{desc}</p><p className="mt-1 text-xs text-muted-foreground">{formatRelative(a.time,locale)}</p></div></div>})}</div>}</CardContent></Card>}
